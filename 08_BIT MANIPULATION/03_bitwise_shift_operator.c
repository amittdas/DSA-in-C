#include <stdio.h>

int main(){
    // Left Shift operator
    // (a << b) = a * (2^b)
    printf("%d\n", (7 << 2));  // 28

    // Right Shift operator
    // (a >> b) = a / (2^b)
    printf("%d\n", (7 >> 2));  // 1

    return 0;
}